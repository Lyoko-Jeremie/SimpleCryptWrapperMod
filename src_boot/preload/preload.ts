(async () => {
    // console.warn('SimpleCryptWrapper preload done', window.modUtils.getNowRunningModName());
    // window.modUtils.getLogger().warn(`SimpleCryptWrapper preload done ${window.modUtils.getNowRunningModName()}`);
})();
